// protocol — contract ABIs, abigen Go bindings, decoded event types, and the
// typed on-chain address book for the Sidiora launchpad (Paxeer chain id 125).
// Lowest-level module: depends on nothing else in the monorepo. [L8]
module github.com/Sidiora-Technologies/KindleLaunch/protocol

go 1.25.0
